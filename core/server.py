"""nGAIA API Server — FastAPI + SSE streaming v0.5.0nn Endpoints (unchanged from v0.4.3 unless marked NEW/UPGRADED):n  GET  /status                            — system health  [UPGRADED: includes runtime engine state]n  GET  /canon/status                      — canon status for UIn  GET  /memory/list                       — session memory for UIn  GET  /gaians/base-forms                 — list all Base Form archetypesn  GET  /gaians                            — list user's personal GAIANsn  POST /gaians                            — spawn a new GAIANn  GET  /gaians/{slug}                     — get a GAIAN's full profilen  POST /gaians/{slug}/remember            — add long-term memory to a GAIAN (legacy)n  POST /gaians/{slug}/memory              — add visible memory via GAIANRuntime  [NEW]n  GET  /gaians/{slug}/runtime-status      — live engine snapshot (neuro/settling/attachment) [NEW]n  POST /session/{session_id}/gaian        — set active GAIAN for sessionn  POST /query/stream                      — SSE query pipeline  [UPGRADED: GAIANRuntime system prompt]nn Architecture change in v0.5.0:n  POST /query/stream now calls GAIANRuntime.process() on every user message.n  The returned RuntimeResult.system_prompt replaces the legacy build_gaian_system_prompt()n  output, wiring all three engines (ConsciousnessRouter → EmotionalArcEngine →n  SettlingEngine) into every LLM call. Legacy GaianMemory paths remain as fallback.nn Canon Ref: C15, C17, C21n Runtime Ref: core/gaian_runtime.py (v0.5.0)n"""