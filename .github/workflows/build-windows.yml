# This workflow is retired. Cross-platform builds are now handled by build.yml.
# Kept as a placeholder to avoid broken branch protection references.
# Safe to delete once build.yml is confirmed stable.
name: ⚠️ Retired — See build.yml
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. Use build.yml for all platform builds."
